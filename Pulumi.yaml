name: eu-captcha
runtime: nodejs
description: eu-captcha app that uses containers
template:
  config:
    aws:region:
      description: The AWS region to deploy into
      default: eu-west-3
    redisPassword:
      description: The Redis password
      secret: true
